#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}

# GOMAXPROCS={{ ansible_processor_vcpus | default(ansible_processor_count) }}

user="{{ gotosocial_system_user }}"
group="{{ gotosocial_system_group }}"

command_args="serve /etc/gotosocial/config.yml"
