#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}

OPTIONS="
  --config-path {{ gotosocial_config_dir }}/config.yml \
  server start"
{#
Usage:
  gotosocial [command]

Available Commands:
  admin       gotosocial admin-related tasks
  completion  Generate the autocompletion script for the specified shell
  debug       gotosocial debug-related tasks
  help        Help about any command
  server      gotosocial server-related tasks
  testrig     gotosocial testrig-related tasks

Flags:
      --account-domain string      Domain to use in account names (eg., example.org, whatever.com). If not set, will default to the setting for host. DO NOT change this on a server that's already run!
      --application-name string    Name of the application, used in various places internally (default "gotosocial")
      --config-path string         Path to a file containing gotosocial configuration. Values set in this file will be overwritten by values set as env vars or arguments
      --db-address string          Database ipv4 address, hostname, or filename
      --db-database string         Database name (default "gotosocial")
      --db-password string         Database password
      --db-port int                Database port (default 5432)
      --db-tls-ca-cert string      Path to CA cert for db tls connection
      --db-tls-mode string         Database tls mode (default "disable")
      --db-type string             Database type: eg., postgres (default "postgres")
      --db-user string             Database username
  -h, --help                       help for gotosocial
      --host string                Hostname to use for the server (eg., example.org, gotosocial.whatever.com). DO NOT change this on a server that's already run!
      --landing-page-user string   the user that should be shown on the instance's landing page
      --log-db-queries             Log database queries verbosely when log-level is trace or debug
      --log-level string           Log level to run at: [trace, debug, info, warn, fatal] (default "info")
      --protocol string            Protocol to use for the REST api of the server (only use http if you are debugging or behind a reverse proxy!) (default "https")
  -v, --version                    version for gotosocial
 #}
